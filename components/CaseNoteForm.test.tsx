import CaseNoteForm from "./CaseNoteForm"

describe("CaseNoteForm", () => {
  it("renders correctly", () => {})

  it("shows the right conditional fields", () => {})

  it("shows an error if submission fails", () => {})
})
